name: Profiles
wysiwyg: true
preview: fields
icon: users
fields:
  titre:
    label: Titre
    type: text
  profiles:
    label: Profiles
    type: structure
    fields:
      image_cover:
        label: Portrait
        type: files
        uploads:
          accept: image/*
        max: 1
        min: 1
        required: true
        image:
          back: black
        size: huge
        width: 1/2
        layout: cards
        query: site.find('images')
      name:
        label: Nom
        type: text
      function:
        label: Fonction
        type: text
      roles:
        label: Rôles
        type: list
