title: Projects

options:
  changeSlug: false
  changeStatus: false
  changeTemplate: false
  delete: false
  duplicate: false
  preview: "https://facenord.max-esnee.com/projets"

sections:
  pages:
    label: Liste des projets
    type: pages
    layout: cards
    image:
      query: page.image_cover.toFile
      back: black
    templates:
      - project
