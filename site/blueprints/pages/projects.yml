title: Projects

options:
  changeSlug: false
  changeStatus: false
  changeTemplate: false
  delete: false
  duplicate: false

sections:
  pages:
    label: Liste des projets
    type: pages
    layout: cards
    image:
      query: page.image_cover.toFile
      back: black
    templates:
      - project
